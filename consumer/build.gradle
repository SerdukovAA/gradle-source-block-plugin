buildscript {
    repositories {
        maven {
            url uri('../repo')
        }
    }
    dependencies {
        classpath group: 'serdukovaa.github.io',
        name: 'source-block-plugin',
        version: '1.0.0-SNAPSHOT'
    }
}

apply plugin: 'source-block-plugin'
